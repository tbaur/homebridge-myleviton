# Scripts

| Script | Purpose |
|--------|---------|
| `test-websocket.js` | Test WebSocket connectivity to My Leviton for real-time device updates |
| `probe-events.mjs` | Capture every raw WebSocket frame (unfiltered) to discover whether physical events like controller button presses are pushed to the cloud |
| `probe-ttl.mjs` | Probe Leviton API token behavior (TTL, metadata, sliding expiry) |
